# --- FONTES (Essencial para NvChad) ---
# Substitua pela fonte que você instalou (ex: JetBrainsMono, Hack, FiraCode)
# Garanta que seja uma "Nerd Font"
font_family      JetBrainsMono Nerd Font
bold_font        auto
italic_font      auto
bold_italic_font auto
font_size        12.0

# --- JANELA & APARÊNCIA (Ajuste para BSPWM) ---
# Remove a barra de título superior (o BSPWM já cuida disso)
hide_window_decorations yes
# Um pouco de margem interna para o texto não colar na borda
window_padding_width 6
# Transparência (0.0 a 1.0) - Ajuste conforme seu gosto
background_opacity 0.95

# --- PERFORMANCE ---
sync_to_monitor yes
# Taxa de atualização (evita flickering)
repaint_delay 10
input_delay 3

# --- ARQUIVO DE TEMA ---
# Isso permite que você troque temas facilmente depois
include ./current-theme.conf
